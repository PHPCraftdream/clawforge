# Checkpoint — 2026-09-14 12:57

## Session summary

This session opened mid-work on task #152 (distinguishing "not-checked" / "could-not-check" /
"failed" in `./clawforge accept`'s report, inside what was then `<local-path>`)
when the user reported that **all data on disk had been accidentally deleted**, leaving only
the running Docker container and (it turned out) a GitHub remote. Investigation found the
live gateway container (`open_claw-gateway-1`) completely intact — its data lives on the
WSL2 virtual disk (`<local-path>`), a separate filesystem from the wiped
Windows `D:` drive, so config, secrets, agent memory and cron history all survived untouched.
Both git repositories (app + framework) were gone from `D:`, but `gh repo list` turned up
`PHPCraftdream/clawforge` — the user confirmed this was the actual, more-advanced lineage of
the same project (different task numbering, `#67–#95` in its own prior checkpoints, richer
layout, generalized providers, CI, dual licensing) rather than a backup of this session's own
`#145–#152` work, and said to treat it as canonical. Cloned it, rebuilt (`npm install` +
`npm run build`), reconnected it to the live instance by hand-writing `apps/openclaw/.env` to
point at the real data dir/port/token, then discovered the framework's `safeName` rejects the
container's real compose-project name (`open_claw`, underscore) at every `--app` resolution —
added `OC_COMPOSE_PROJECT` as a new, narrowly-scoped `.env` override so Docker's own (wider)
naming rule could be satisfied without recreating the container. Rebuilt `recipes/confluence-mock`
byte-for-byte from the target's own file mirror (found and fixed a Windows-pipe corruption
bug in the recovery process itself — filenames were getting truncated by CRLF translation
through a `wsl.exe | tar` pipe; fixed by routing through an intermediate file on disk instead
of a live pipe) and from live `cron list`/`mcp list`/`agents list` output; verified with a real
`./clawforge plan` (nothing to do) and `./clawforge accept` (6/6 passed). Committed and pushed
that `OC_COMPOSE_PROJECT` change (`e9d5b3d`) at the user's explicit request. The user then
asked for framework and app to live in **sibling directories** rather than one nested inside
the other, with the app linking to the framework via a real npm symlink — restructured to
`<local-path>` (framework) and `<local-path>` (app,
`node_modules/@clawforge/framework` → `../clawforge/tools/framework` via `file:` dependency),
then renamed the confusingly-similar directory names for clarity (`open_claw`→`clawforge`),
working around a genuine Windows file-lock by cloning locally into the new name instead of
`mv`-ing. Most recently, the user supplied an external code review of the framework at commit
`e9d5b3d` reporting five defects; each was independently verified by reading the actual code
(and, for two of them, by fetching OpenClaw's real `zod-schema.core.ts`/`zod-schema.root-shape.ts`
from GitHub to confirm the exact schema constraints rather than guessing) and all five were
confirmed real. Registered as tasks `#153`–`#158` and fixed one at a time, each with a
regression test proven to fail against the pre-fix code via `git stash`. All fixes, tests, the
full check suite (59/59), `tsgo`, `oxlint`, `npm run build` and `npm run pack:check` are green.
**Nothing from this last batch (`#153`–`#158`) is committed** — the user was asked "коммитить и
пушить?" and responded with `/checkpoint` instead of answering, so that question is still open.

## Active goal

none — no `/goal` Stop hook is in force this session.

## TaskList

### in_progress
- #152 Tell "not checked" apart from "could not check" and "checked, not working"  (interrupted by the disaster; see Open questions — the recovered clawforge codebase's `./clawforge accept` already prints `NOT-CHECKED` for a skipped model-calling check, so this task's actual remaining scope against the *current* codebase needs re-assessing, not assumed)

### pending
(none)

### recently completed
- #158 Verify and commit the CI fix (npm ci --include=dev) — verified correct; NOT committed, see summary
- #157 set try must save and restore the compose-project override, not just the deployment directory
- #156 Separate deployment identity from compose-project override (fixes deploy.ts's --app corruption)
- #155 Fix bootstrap/set-try ordering: apply desired-state before configure-provider for new custom providers
- #154 Fix secrets requirements: don't demand an env API key oauth/file-ref/local providers don't need
- #153 Fix configure-provider: --provider must not absorb every discovered provider
- #151 Roll back to the previous set, not just to a configuration file
- #150 Add ./oc set try: a throwaway instance, installed and accepted in one operation
- #149 Track what the framework owns, so removal from a set means something
- #148 Let plan and apply install from a set artifact instead of the working tree

## Decisions

- Treated the GitHub `clawforge` remote as the canonical project state rather than trying to
  reconstruct `#145`–`#152`'s exact prior local commits from session memory — explicit user
  call, made once the divergent lineage was surfaced and explained.
- Introduced `OC_COMPOSE_PROJECT` (a new `.env` override, validated against Docker's own wider
  project-name alphabet) instead of recreating the live container under a `safeName`-compliant
  project name — avoids downtime and matches the project's own established "derive a compliant
  value, don't loosen the guard" pattern from earlier in this project's history.
- Restructured to sibling directories with a real npm `file:` symlink (the "installed
  dependency" mode the framework already documented but this session hadn't been using) rather
  than the framework's own colocated `apps/<name>` model — explicit user request for clearer
  separation.
- For two of the five review findings (bootstrap/set-try call ordering re bootstrap's own
  ordering-in-set-try, and set-try's compose-override restore), used a lightweight **structural**
  regression test (asserts call order via a real stubbed `bootstrap()` run for the primary case,
  source-text order checks for the harder-to-stub `set-try.ts` echo of the same pattern) rather
  than a full behavioral simulation requiring a real checksum-valid set artifact — judged
  disproportionate cost for confirming a mechanism already proven at the primitive level
  elsewhere. Documented as such in the check files' own comments, not left implicit.
- Did not commit or push the `#153`–`#158` fixes without being asked again — the earlier
  explicit "commit and push" instruction covered only the `OC_COMPOSE_PROJECT` change, not
  this separate, later batch of review fixes.

## Open questions

- **Commit/push `#153`–`#158`?** Asked directly at the end of the previous turn; the user
  invoked `/checkpoint` instead of answering. Still open.
- **What does #152 actually still need, against the *current* (recovered) codebase?** The
  live `./clawforge accept` run during recovery already printed a `NOT-CHECKED` status for a
  skipped `usesModel` check — the recovered lineage may have already built some or all of the
  three-way not-checked/could-not-check/failed distinction #152 was chasing before the
  disaster. Needs a fresh read of `tools/framework/commands/orchestration/accept.ts` (note the
  path itself has moved since the pre-disaster session) against #152's own "Cover:" bullets
  before resuming or closing it — not an assumption either way.
- Whether the sibling-directory restructure should also move/update anything in
  `<local-path>` (the app) beyond what was already done (`.env` fixed,
  recipes/config copied, `package.json` dependency path updated) has not been re-examined
  since the `#153`–`#158` framework-only fix round — the app directory itself was not touched
  during that round and should still be consistent, but this has not been re-verified after
  the framework's changes (e.g. a fresh `./clawforge plan`/`accept` from `openclaw/` against
  the now-fixed framework has not been re-run since `#158`).

## Repo state

`<local-path>` (framework):
```
 M .github/workflows/ci.yml
 M tools/checks/foundation/core/deployment-names.check.ts
 M tools/checks/foundation/core/requirements.check.ts
 M tools/checks/foundation/providers/provider.check.ts
 M tools/checks/runtime/service/deploy.check.ts
 M tools/checks/sets/lifecycle/set-try.check.ts
 M tools/framework/commands/lifecycle/bootstrap.ts
 M tools/framework/commands/management/provider.ts
 M tools/framework/commands/sets/set-try.ts
 M tools/framework/core/context.ts
 M tools/framework/runtime/deployment.ts
 M tools/framework/runtime/runtime-docker.ts
 M tools/framework/service/secrets.ts
?? tools/checks/runtime/lifecycle/
```
```
e9d5b3d Let a deployment's compose project name differ from its directory name
8f15c8c Reorganize source and generalize providers
647d8a7 Prepare ClawForge for Open Source publication
```

`<local-path>` (app) — not re-checked in this pass; last known clean
against framework commit `e9d5b3d`, before the `#153`–`#158` fixes above.
