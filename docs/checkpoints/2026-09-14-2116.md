# Checkpoint — 2026-09-14 21:16

## Session summary

This session picked up right after a `/resume` of an earlier disaster-recovery session (Windows drive wipe, framework recovered from GitHub, then restructured into sibling `clawforge`/`openclaw` directories). Since that resume, the entire session has been one long, repeating cycle: the user pastes an external code review of the ClawForge framework (a self-hosting toolkit for OpenClaw), written in Russian, citing specific file:line defects; I verify each finding against the actual current code (never trusting the review blindly — several findings turned out to be about code from *my own* previous fix round, and a couple of cited findings did not hold up exactly as worded but the underlying defect was still real), fetch OpenClaw's real docs/schema from GitHub or docs.openclaw.ai when a finding depends on an external contract (JSON5 config support, the `--all` flag for `cron list`, SecretRef shapes, etc.) rather than guessing, register each confirmed defect as a task, fix it, and prove the fix with a regression test that is shown to fail against the pre-fix code via `git stash` before being confirmed passing against the fix. This pattern has now run six full rounds (tasks #153–#158, #159–#162 [in a Q&A about the same review], #163–#169, #170–#175, #176–#180, #181–#184, #185–#188 — the numbering is continuous across rounds). After every round: full check suite (`npm run check`, now 60/60 check files), `npm run format:check` (tsgo + oxlint), `npm run build`, `npm run pack:check` — all green — then commit (message summarizing the round, `Co-Authored-By: Claude Sonnet 5`), and push only when the user explicitly says so in that exact turn (asked every time, never assumed carried-forward). The most recent round (#185–#188, commit `0e7325e`) was committed per the user's explicit "коммит" — but NOT pushed (the user only said "коммит" this time, not "коммит и пуш"), so `0e7325e` is one commit ahead of `origin/main` (last pushed: `375eaf8`). A `json5` runtime dependency was added in an earlier round (#179) — the framework's first-ever production dependency — after asking the user to choose between that and a hand-rolled comment-stripping parser; user picked the real `json5` package. Task #152 (distinguishing not-checked/could-not-check/failed in `accept.ts`) has sat `in_progress` and untouched all session — it predates the disaster and was never revisited after the recovery, since every turn since has been entirely consumed by review-fix rounds.

## Active goal

none — no `/goal` Stop hook is in force this session.

## TaskList

### in_progress
- #152 Tell "not checked" apart from "could not check" and "checked, not working" — untouched this entire session; needs a fresh look at the current `tools/framework/commands/orchestration/accept.ts` against its original "Cover:" acceptance bullets before resuming, not an assumption that it's still relevant as originally scoped.

### pending
(none)

### recently completed
- #188 cron lookups in provision-agent.ts must pass --all to see disabled jobs
- #187 clawforge init must refuse when .env or config/desired-state.json already exist
- #186 configure-provider and secrets --apply must take the instance lock
- #185 rollback --set must refuse when no config snapshot is available to restore
- #184 declaredState must only silently tolerate ENOENT, not every readFile failure
- #183 publish.yml: install dependencies before running tests
- #182 rollback --set must re-verify the installed set under the lock (race guard)
- #181 runningImageUnconfirmed must treat a fully undefined image identity as unconfirmed too
- #180 inspect: a desired-state.json that exists but fails to parse must be a blocking finding
- #179 verify: parse the archive's embedded config as JSON5, refuse on parse failure

(35 more tasks completed earlier this session and in prior sessions: #145–#178, all set-lifecycle/review-fix work — see TaskList for the full history.)

## Decisions

- Every review-fix task gets a regression test proven to fail on pre-fix code via `git stash push -- <file>` / run / `git stash pop`, never merely "added and passes" — this was re-affirmed as non-negotiable across all six rounds this session.
- Chose the real `json5` npm package over a hand-rolled comment/trailing-comma stripper for #179, after explicitly asking the user — first production dependency this framework has ever taken.
- `mcpServerMatches()`, `runningImageDigest()`/`runningImageUnconfirmed()` and similar small helpers are exported and shared across every consumer (provision-agent, accept, inspect, apply, rollback) rather than duplicated per-file, each time a review finding showed the same root cause reachable from multiple call sites.
- Commit and push remain two separately-gated actions the user must request explicitly each time; "коммит" alone (this turn) was treated as commit-only, matching the standing rule never to push without an explicit, separate ask.
- When a review's own file:line citations pointed at code already superseded by an earlier fix in the same session, verified against the CURRENT code rather than assuming the review was stale or wrong outright — in every round so far the underlying defect was still real even when the exact citation had drifted.

## Open questions

- Push commit `0e7325e` (and everything back through `7f25040`, all currently unpushed except through `375eaf8`)? Not yet asked this turn — the user said only "коммит".
- Is there a seventh review round coming, or should #152 be picked back up next? Not asked; the pattern this session has been reactive (wait for the next pasted review) rather than user announcing "no more reviews."
- #152's actual remaining scope against the current (recovered + six-rounds-patched) `accept.ts` has still never been re-examined — flagged as open since the very first checkpoint this session and still true.

## Repo state

```
?? docs/checkpoints/2026-09-14-1257.md
```
```
0e7325e Fix four defects from a sixth external framework review
375eaf8 Fix four defects from a fifth external framework review
db52f0a Fix five defects from a fourth external framework review
220ab77 Fix six defects from a third external framework review
7f25040 Fix seven defects from a second external framework review
```
