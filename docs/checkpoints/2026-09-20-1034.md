# Checkpoint — 2026-09-20 10:34

## Session summary

Continuation of the same long session (previous checkpoint: `docs/checkpoints/2026-09-20-0854.md`
— full earlier narrative there: Telegram/tor-socks5 recipe deployment, the privoxy HTTP-bridge
workaround for Node's experimental SOCKS5 dispatcher, the multi-hour `api.z.ai` DNS dead-end
investigation across WSL/Docker Desktop internals, the `wsl -u root` security clarification, and
`./clawforge host`/`exec`/`recipe diagnose`/`shouldFollow()` being built and — unlike this
session's earlier framework work — actually committed and pushed to `origin/main` at commit
`cac7162` after the user explicitly asked "коммит, пуш, зеленый ci"). CI on that push was
confirmed green via `gh run watch` before this checkpoint's own work began.

**This checkpoint's new work: clearing the remaining backlog via sequential `/wrush` runs.**
The user asked to implement all remaining pending/in-progress tasks (`#152`, `#267`, `#268`,
`#269`, `#277`) via `/wrush`, explicitly "all of them, but one at a time" (not parallel) — a
deliberate sequencing choice given real file-overlap risk this session already hit once (the
`tools/checks/security/credentials/` 8-entries layout collision from two independent /wrush
runs touching the same directory, from an earlier session leg).

**#267 (`apply-config --dump`) — done, verified, merged, still UNCOMMITTED.** Delegated to a
`/wrush` sub-agent in `worktrees/desired-state-dump` (removed after merge). Reconstructs
`config/desired-state.json` from a live instance's `openclaw.json` when the operator's own copy
was lost. Design decision made before delegating (grounded in reading `integration/init.ts`'s
own scaffold template and `observe.ts`'s `declaredState()`): recovers exactly three
well-known/curated paths — `gateway.mode`, `gateway.bind`, `agents.defaults.model.primary` — via
the existing `valueAt()`/`readLiveConfigOrThrow()` helpers, omitting (never guessing) a path the
live config never set, and explicitly refusing to fold recipes into the reconstructed file
(desired-state.json is strictly a `{path,value}[]` `config set --batch-file` payload — no
established way to declare a recipe list inside it — the sub-agent found and cited
`desiredStateShapeError` in `set/ownership/validate.ts` as the concrete evidence for this,
independently confirming the fork this session had already flagged as open). Mirrors
`secrets --dump`'s existing store-refusal/`--force` contract exactly. Zero-trust verified
personally: full diff read, an independent break/restore (inverted the omission-guard logic,
confirmed 2 tests failed, restored) distinct from the sub-agent's own claimed sabotage test, full
gate green in both the worktree and, after hand-copying the 4 files, in the primary checkout
again.

**#268 (`.env` connection-facts recovery) — IN FLIGHT right now**, delegated to a `/wrush`
sub-agent in `worktrees/env-recovery` (not yet merged, not yet verified — this checkpoint is
being written while that background run is still executing). Design handed to the sub-agent
before launch (grounded in reading `runtime-docker.ts`'s existing `runningEnvironment()` and
`docker-compose.yml`'s exact mount/port/label shapes myself first, same investigation discipline
as #267): a new optional `Runtime.runningConnectionFacts()` capability, mirroring
`runningEnvironment()`'s exact introspection pattern, extracting from one `docker inspect`
call — `OC_DATA_DIR` from the `/home/node/.openclaw` mount's `Source` (minus the trailing
`/config`), `OPENCLAW_GATEWAY_PORT` from `.NetworkSettings.Ports["18789/tcp"][0].HostPort`,
`OC_COMPOSE_PROJECT` from the `com.docker.compose.project` label, `OPENCLAW_IMAGE` from
`.Config.Image` (explicitly NOT the top-level `.Image` field, which is the resolved digest, not
the original tag). Told to reuse `upsertEnvValue`/`replacePrivateFile` for the actual `.env`
write (same mechanism `secrets --apply` already uses, because `.env` mixes non-secret connection
facts with the real `OPENCLAW_GATEWAY_TOKEN` secret in the same file) and to explicitly refuse
the "totally absent `.env`" case as a stated, inherent, unfixable limit (reaching the target to
inspect it already requires a working `.env`). Command surface left as the sub-agent's own
judgment call with a strong suggested default (`./clawforge recover-env`), to be reviewed
zero-trust like every other fork this session left open.

**#269 and #152 not started yet** — #269 (docs tying `secrets --dump` + `desired-state --dump` +
`pull --with-secrets` together) is next after #268 lands, since it names #267's real command by
name and should describe what's actually shipped. #152 (the acceptance three-outcome taxonomy —
`not-checked`/`could-not-check`/`failed`, replacing today's conflated `skipped`) is queued last;
it is a pre-existing, unrelated-subsystem task the user chose to include in this same batch
despite no file overlap with the dump/host work, specifically to get it off the backlog while a
`/wrush` sequencing rhythm was already running.

**#277 (optional `host --topology`)** remains explicitly deferred/optional, included in the
user's "all 5" batch but genuinely the lowest-priority one — likely last in the queue after #152,
or dropped if the batch's momentum runs out, since its own task description already says "may
turn out small enough to not need its own task."

## Active goal

None — no `/goal` Stop hook is in force in this session.

## TaskList

### in_progress
- #268 .env connection-facts recovery from docker inspect (port/dataDir/compose
  project/image) — a /wrush run is executing in `worktrees/env-recovery` right now; not yet
  merged or verified as of this checkpoint

### pending
- #269 Docs: tie secrets --dump + desired-state --dump + pull --with-secrets into one
  recovery story — next after #268 lands
- #152 Tell "not checked" apart from "could not check" and "checked, not working" — queued
  after #269, per the user's explicit "all 5, one at a time" instruction
- #277 Optional follow-up: ./clawforge host --topology snapshot — last in the queue, genuinely
  optional

### recently completed
- #267 desired-state --dump (verified, merged into primary checkout, UNCOMMITTED — see summary)
- #276 Add ./clawforge host <context> -- <cmd...> (committed as `cac7162`, pushed, CI green)
- #273/#274/#275 (shouldFollow() fix / ./clawforge exec / ./clawforge recipe diagnose),
  committed together as `3efcf2c`, pushed, part of the same earlier push as `cac7162`

## Decisions

- `/wrush` batch of 5 remaining tasks run strictly sequentially, never in parallel — the
  user's own explicit call ("все сразу, но по очереди"), reinforced by this session's own
  earlier real collision (the 8-entries layout violation from two parallel /wrush runs touching
  the same directory independently).
- #267: recipes explicitly left out of the reconstructed desired-state.json (it is a
  `{path,value}[]` batch-file payload with no established way to declare a recipe list) —
  confirmed correct independently by the sub-agent, matching this session's own earlier
  open-question framing of the same fork.
- #267/#268 both reuse existing, already-battle-tested machinery deliberately rather than
  inventing new mechanisms: `valueAt()`/`readLiveConfigOrThrow()` (already used by
  `observeConfig()`/`secrets --apply`) for #267; the `runningEnvironment()`-style single
  `docker inspect` call for #268. This mirrors the exact "reuse existing machinery" principle
  already decided for the original `secrets --dump` (#266) work.
- Every /wrush merge in this batch gets the same zero-trust treatment already established:
  read the full diff personally, independently break-and-restore a DIFFERENT piece than
  whatever the sub-agent itself claims to have sabotage-tested, re-run the full gate personally
  in both the worktree and the primary checkout after copying files by hand (never `git merge`).

## Open questions

- #268's actual command name/surface — the sub-agent was given a strong suggested default
  (`./clawforge recover-env`) but explicit latitude to pick something else if a clearly better
  existing-command fit turns up; not yet reviewed, since the run was still in flight when this
  checkpoint was written.
- Whether #267+#268 (currently uncommitted, plus whatever #268 lands as) should be committed
  and pushed together, or #268 gets its own commit — no instruction given yet for this batch;
  the previous round's commits were one commit per /wrush-delegated feature (`cac7162` for
  #276 alone), so that precedent likely continues unless told otherwise.
- #152's actual implementation approach is entirely undesigned as of this checkpoint — no
  investigation into its target files (likely `accept.ts`/`gather.ts`/the `StructuredResult`
  contract) has happened yet; when its turn comes, the same investigate-before-delegating
  discipline used for #267/#268 should be applied before writing its /wrush prompt.

## Repo state

```
 M README.md
 M tools/checks/runtime/convergence/apply-config.check.ts
 M tools/framework/commands/interface/groups/openclawCommands.orchestration.ts
 M tools/framework/commands/orchestration/config.ts
```
```
cac7162 feat: add ./clawforge host for cross-platform operator-machine exec
3efcf2c feat: recipe safety, secret recovery, and CLI diagnostics tooling
c028c76 docs: record series 2 round 1 framework and MCP review
8bb4bf6 feat: centralize deployment secret delivery
412ebe8 feat: expose conditional structured recipe results
```

Active worktree at checkpoint time: `worktrees/env-recovery` (branch `env-recovery`, task #268,
/wrush run in flight, not yet merged).
