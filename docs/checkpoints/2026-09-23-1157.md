# Checkpoint — 2026-09-23 11:57

## Session summary

Продолжение из чекпоинта `2026-09-23-0853.md`. Три ветки работы этого сегмента:

**1. CI-фикс #3 добит.** `ci-raw-archive-test-escalation` (sidecar-private raw-archive
эскалация в `private-history-restore.check.ts`) прошёл полный гейт в worktree и в primary
checkout (96/96), закоммичен и запушен как `3c75e93`. Worktree удалён. Это закрывает исходный
запрос "коммит, пуш, зелёный CI" — три реальных CI-only бага найдены и починены
последовательно (chown escalation → archive read escalation → raw-archive test escalation),
каждый вскрывался следующим после починки предыдущего.

**2. Раунд 4 полностью выполнен и смержен (все 13 находок: `#310`–`#322`).** 8 голов
кластеров прошли через `@sh`-агентов (`isolation: worktree`), как и планировалось после
блокировки rush пиковыми часами. Когда пользователь спросил "почему задачи остались?"
(`P1-05, P2-01, P2-02, P2-06, P3-01` — 5 chain-зависимых находок ждали 12:00), явно попросил
запустить их сейчас через `@sh` вместо ожидания — выполнено, 12:00-напоминалка (cron `3d016021`)
отменена.

Каждый агент проверялся по полной zero-trust дисциплине: чтение диффа, независимая мутация
(ломаю фикс → подтверждаю что новый регресс-тест реально падает → откатываю → подтверждаю
зелёный), затем ручной мерж (никогда `git merge`).

**Найдено и исправлено ПОПУТНО (не находки аудита, а инфраструктурные проблемы, вскрытые
процессом верификации):**

- **Флейк** в `security/private-paths-ledger/serialization.check.ts` — фиксированные 10
  итераций `setImmediate` в `settle()`-хелпере не гарантировали, что первая мутация успела
  войти в свой merge (реальный fs I/O, не микротаск) под нагрузкой от ~8 параллельных
  `npm test`. Почин: заменил фиксированный `settle()` перед первой проверкой на
  условие-based `untilEntered("a")` (тот же паттерн, что уже использовался для b/c).
  Верифицирован 5 прогонами.
- **Два скрытых конфликта мержа** между агентами, писавшими в один файл, не зная друг о
  друге (worktree'ы ветвились от разных промежуточных состояний primary checkout):
  - `backup.check.ts`: P1-02 (leaveStopped-тесты) был затёрт при мерже P1-01 — восстановлено
    вручную (реконструировал 3 теста по памяти мутационной проверки, снова подтвердил
    мутацией).
  - `recipe.check.ts` (integration/agent): P2-04 (serviceStates-стаб) был затёрт при мерже
    P2-03 — восстановлено добавлением `serviceStates()` в общий стаб.
  - После этого для ВСЕХ последующих мержей в этот же файл (P2-06, P3-01) — сверял с текущим
    primary ПЕРЕД копированием, применял диффы вручную через Edit, а не слепым `cp`.
- **Реальная регрессия от P1-05** (не флейк, детерминированная): `dist/.env.example`
  (build-артефакт `npm run build`, untracked, но байт-в-байт идентичен трек-файлу
  `tools/framework/.env.example`) ложно блокировал deploy — tracked-only exemption не
  покрывала untracked-копию с идентичным содержимым. Почин: `collectSensitiveCheckoutNames`
  теперь дополнительно проверяет git blob hash (`sha1("blob "+len+"\0"+content)`) untracked-
  кандидата против набора хешей всех трек-файлов (`git ls-files -s`), лениво — только когда
  первая, дешёвая path-проверка не прошла. Добавлена регрессия (untracked-копия не
  блокируется, НО untracked-файл БЕЗ трек-двойника рядом — всё ещё блокируется).
- **Превышение лимита строк** `recipe.check.ts` (755/700 после накопления P1-01+P2-03+P2-04
  правок) — блок регрессий P3-01 (malformed ports/variables) перенесён в
  `recipe-readiness.check.ts` (уже существующий файл того же раунда с запасом места),
  `recipe.check.ts` подрезан до ровно 700.

**Финальный чистый прогон (после всех фиксов):** 101/101 check-файлов, typecheck чист,
format:check (typecheck+lint) чист. Дерево НЕ закоммичено (39 изменённых файлов) — как и
раунды 2/3, ждёт явного запроса пользователя.

Babysit-цикл (`001c330b`) сам себя удалил при опустевшем TaskList — весь `#310`–`#322`
`completed`. Никаких активных фоновых процессов, cron-джобов, worktree на момент чекпоинта.

## Active goal

Нет. `/goal` Stop hook не в силе.

## TaskList

Пусто — все задачи раунда 4 завершены и закрыты babysit-тиком.

## Decisions

- Пользователь явно разрешил запустить 5 chain-зависимых находок раунда 4 через `@sh` СРАЗУ,
  не дожидаясь 12:00/rush — 12:00-cron отменён.
- Каждый обнаруженный побочный баг (флейк, 2 конфликта мержа, false-positive P1-05, лимит
  строк) чинился НА МЕСТЕ, в этом же ходе — по стандартному правилу "флейк/падение чинится
  сразу", даже когда причина не в самой находке аудита, а в процессе верификации/мержа.
  Content-hash подход (git blob hash) выбран вместо блокирующего "просто исключить dist/ по
  имени" — принципиальнее (не ослабляет защиту для НЕпохожих на трек-файлы секретов),
  безопаснее (сильная content identity, не probablistic).
- После двух конфликтов мержа установлена практика: перед копированием диффа субагента —
  сверять затрагиваемый файл с ТЕКУЩИМ (не базовым для агента) состоянием primary checkout;
  при расхождении — применять дифф вручную через Edit, никогда слепой `cp`.

## Open questions

Нет. Единственный открытый вопрос предыдущего чекпоинта ("что делать с 5 chain-задачами")
закрыт явным ответом пользователя в этом сегменте.

## Repo state

```
(39 файлов изменено, не закоммичено — round 2/3/4 фиксы + P1-05 follow-up фикс)
```
```
3c75e93 fix: escalate raw-archive test helper's tar read for locked-down sidecar-private
382b086 fix: escalate createArchive's tar read for a locked-down auth-secrets
1140674 fix: escalate chown to a fixed owner by identity, not directory writability
be7a701 fix: address XA audit rounds 2 and 3 findings
28d54e6 docs: record XS audit round 4
ada7d3a docs: record XA audit round 3
```

Worktree'ы: ни одного (все agent-worktree и ручные worktree удалены после мержа).
