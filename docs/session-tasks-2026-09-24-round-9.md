# Сессионные задачи по аудиту раунда 9

Основание: [отчёт XXS, раунд 9](review-2026-09-24-xxs-round-9.md).
Статус относится к текущему рабочему дереву; изменения ещё не опубликованы.

| Задача | Приоритет | Статус | Результат |
| --- | --- | --- | --- |
| R9-01: tar-исключения при метасимволе в data root | P1 | закрыта | Литеральное имя root экранируется во всех профильных правилах; `migrate` и `share` проверяются перед публикацией. |
| R9-02: ошибка Docker при определении состояния | P1 | закрыта | Ненулевой код probe прерывает холодный backup до tar. |
| R9-03: невалидный manifest работающего sidecar | P2 | закрыта | Строгое обнаружение проверяет stack и отказывает, если живой sidecar нельзя безопасно quiesce. |
| R9-04: существующий query у relative import | P2 | закрыта | Loader читает версию через URL-параметры; вложенный helper обновляется в той же сессии. |
| R9-05: локальные `#imports` в hook | P2 | закрыта через отказ | Такие импорты отклоняются до исполнения, пока их import map нельзя безопасно включить в freshness graph. |
| R9-06: ошибка перечисления при backup rotation | P2 | закрыта | Пустой список отделён от сбоя `find`; ошибки удаления тоже сообщаются. |
| R9-07: обещание свободного порта | P3 | закрыта с ограничением | Порт-кандидат выбирается случайно; bootstrap проверяет активные Docker-публикации. Конфликт с другим процессом и гонка до bind остаются ответственностью Docker. |
| R9-R1: guard с удалённым владельцем | риск | описан | В архитектуре зафиксирована безопасная процедура оператора; автоматический takeover без доказательства остановки другого хоста не допускается. |
| R9-R2: разбор ссылок GNU tar | риск | закрыт | Разбор C-quoted listing исправлен и проверен реальным архивом; выход за пределы каталога не подтверждён. |

Известный инфраструктурный пробел R8-09 — полный Windows → WSL → Docker в CI — остаётся
открытым до появления оснащённого self-hosted runner.

Локально прошли 112 check-файлов, `tsgo --noEmit`, Oxlint, build, pack dry-run и
actionlint. Удалённый CI требует отдельного коммита и push.
